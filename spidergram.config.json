{
  "spider": {
    "urlOptions": {
      "selectors": {
        "header": { "selector": "header", "linkSelector": "a" },
        "main": { "selector": "main", "linkSelector": "a" },
        "footer": { "selector": "footer", "linkSelector": "a" }
      }
    }
  }
}